import { Box } from "@mui/material";
import React from "react";

const TimelineTrack = () => {
  return (
    <Box
      sx={{
        opacity: 0.5,
        position: "absolute",
        border: 1,
        borderRadius: 2,
        width: "100%",
      }}
    />
  );
};

export default TimelineTrack;
